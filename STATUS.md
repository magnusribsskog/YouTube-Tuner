# Status — feature/thematic-intelligence

**What this is:** v4.1 semantic heuristic pipeline — density throttling by
cluster, LLM-derived local heuristics, ClusterMap with half-life decay.

**State:** not started. Branch exists ahead of the work.

**Blocked on (all required before implementation begins):**
1. Hardening phase complete
2. Lifecycle-respecting collapse in place (publication gate — see experiment/lifecycle-collapse)
3. Autoplay target tracking implemented and producing data
4. Re-appearance rate tracking implemented and producing data

See ROADMAP on main for full spec.
